apiVersion: v1
kind: Service

metadata:
  name: chatbot-service
  namespace: online-shop-ns

spec:
  type: ClusterIP
  selector:
    app: ollama
  ports:
    - protocol: TCP
      port: 11434
      targetPort: 11434
      name: http
